namespace Exercises;

/// <summary>
/// Exercise 17: Zipping Two Arrays Together
/// Use Zip to combine an array of first names and an array of last names into a single
/// "First Last" string array.
/// </summary>
public static class Exercise17
{
    public static IEnumerable<string> ZipFullNames(string[] firstNames, string[] lastNames)
    {
        // TODO: use Zip() with (first, last) => $"{first} {last}"
        throw new NotImplementedException();
    }
}
