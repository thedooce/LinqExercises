namespace Exercises;

/// <summary>
/// Exercise 40: Splitting a List into Chunks
/// Given a large list of integers, split it into smaller sub-lists (chunks) of exactly 5
/// elements each, using the Chunk operator.
/// </summary>
public static class Exercise40
{
    public static IEnumerable<int[]> ChunkList(List<int> numbers, int chunkSize = 5)
    {
        // TODO: implement
        throw new NotImplementedException();
    }
}
