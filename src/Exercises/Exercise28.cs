namespace Exercises;

/// <summary>
/// Exercise 28: Counting Vowels with LINQ
/// Given a string, count how many vowels (a, e, i, o, u) it contains using LINQ.
/// </summary>
public static class Exercise28
{
    public static int CountVowels(string text)
    {
        // TODO: use Count() with a predicate over "aeiouAEIOU".Contains(c)
        throw new NotImplementedException();
    }
}
